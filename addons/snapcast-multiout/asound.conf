# ---------- USB Audio Device Configuration ----------
# Creative Sound Blaster Play! 3 detected as card 2
pcm.!default { type hw; card 2 }
ctl.!default { type hw; card 2 }

# ---------- Alternative configurations (comment out above and uncomment below if needed) ----------
# For card 1 (previous configuration):
# pcm.!default { type hw; card 1 }
# ctl.!default { type hw; card 1 }

# For card 0 (built-in audio):
# pcm.!default { type hw; card 0 }
# ctl.!default { type hw; card 0 }

# ---------- UMC1820 mappings (enable when it arrives) ----------
# pcm.umc_raw { type hw; card "UMC1820"; device 0; channels 10 }

# pcm.umc_out_12 { type route; slave.pcm "umc_raw"; slave.channels 10; ttable.0.0 1; ttable.1.1 1; }   # Main L/R
# pcm.umc_out_34 { type route; slave.pcm "umc_raw"; slave.channels 10; ttable.0.2 1; ttable.1.3 1; }   # Line 1/2
# pcm.umc_out_56 { type route; slave.pcm "umc_raw"; slave.channels 10; ttable.0.4 1; ttable.1.5 1; }   # Line 3/4
# pcm.umc_out_78 { type route; slave.pcm "umc_raw"; slave.channels 10; ttable.0.6 1; ttable.1.7 1; }   # Line 5/6
# pcm.umc_out_910{ type route; slave.pcm "umc_raw"; slave.channels 10; ttable.0.8 1; ttable.1.9 1; }   # Line 7/8
